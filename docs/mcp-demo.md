# MCP-демо: инструменты тренера через tulpar-mcp

`tulpar-mcp` — stdio-сервер MCP (`python -m tulpar_ai.mcp_server`). Он даёт AI-ассистенту тренера (Claude Desktop,
Claude Code) пять инструментов сервиса. Сервер — тонкий клиент HTTP API: ходит в `SERVICE_URL` с `INTERNAL_SECRET`
от имени тренера, поэтому права и маскирование данных клиентов проверяет сам сервис. Пример конфигурации лежит в
`mcp/claude_desktop_config.example.json`.

## Как проверяли

1. Claude Code в headless-режиме с временным конфигом только для этого сервера:

   ```bash
   claude -p --mcp-config <временный.json> --strict-mcp-config --allowedTools "mcp__tulpar-mcp" \
     "Я тренер. Покажи моих клиентов и предложи Айдару облегчить план из-за боли в колене."
   ```

   Claude Code запустил сервер: `tulpar-mcp` в статусе `connected`, найдено 5 инструментов (`list_clients`,
   `get_client_context`, `search_exercises`, `propose_program_change`, `list_pending_proposals`). До вызовов
   инструментов прогон не дошёл: CLI на этой машине не прошёл авторизацию (истекла OAuth-сессия). Временный
   конфиг удалён, глобальные настройки Claude не менялись.

2. Поэтому транскрипт ниже снят официальным MCP-клиентом на Python (`tools/mcp_demo.py`). Он запускает тот же
   сервер по stdio, как это делает MCP-хост, получает список инструментов и вызывает их в порядке, который сервер
   советует в своих `instructions`:

   ```bash
   python -m tulpar_ai                                  # сервис
   python tools/mcp_demo.py --base http://localhost:8089
   ```

## Транскрипт (локальный сервис, demo-режим, 25.09.2026)

Запрос тренера: «Облегчи план из-за боли в колене: убери ударную и глубокую осевую нагрузку на колено».

```text
MCP-демо: tulpar-mcp (stdio) → http://localhost:8099
сервер: tulpar-mcp, инструментов: 5
  - list_clients: Клиенты тренера: id, имя, цель, уровень, место тренировок, есть ли ограничение по здоровью, активная программа
  - get_client_context: Контекст клиента для решений по программе: профиль (цель, уровень, место, дни), заметка тренера об
  - search_exercises: Поиск в каталоге упражнений Tulpar. muscle_group: ноги, спина, грудь, плечи, руки, пресс, кор, кардио,
  - propose_program_change: Попросить AI-коуча подготовить черновик изменения программы клиента. Черновик собирается по Skill
  - list_pending_proposals: Очередь тренера: черновики изменений программ, ждущие решения, и эскалации (сообщения клиентов о боли,

→ list_clients()  [0.1 с]
  Айдар Ким (9b7e3eaa): цель cut, место gym, ограничение по здоровью: да
  Дана Сейткали (e9d058b1): цель keep, место home, ограничение по здоровью: нет

→ get_client_context(client_id="9b7e3eaa-00df-554a-8c3d-83edffe6ca5e")  [0.0 с]
  заметка тренера: Жалуется на левое колено после бега. Глубокие приседания со штангой и прыжки пока исключаем, жим ногами — только в неполной амплитуде.
  план «PPL — тяни / толкай / ноги»: Push (грудь-плечи-трицепс) (6 упр.); Pull (спина-бицепс) (6 упр.); Legs (ноги) (6 упр.)

→ search_exercises(muscle_group="ноги", avoid_problems_with="колено")  [0.0 с]
  найдено 30: Румынская тяга, Фронтальные приседания, Приседания в смите, Жим ногами узкой постановкой, Разгибания ног в тренажёре, Сгибания ног лёжа, Сгибания ног сидя, Выпады со штангой …

→ propose_program_change(client_id="9b7e3eaa-00df-554a-8c3d-83edffe6ca5e", request="Облегчи план из-за боли в колене: убери ударную и глубокую осевую нагрузку на колено", wait_seconds=180)  [15.1 с]
  статус: pending, черновик 59e67d98
  summary: Облегчаем день ног: меняем приседания со штангой на неглубокий жим ногами узкой постановкой, а обратную часть дна добавляем гиперэкстензию вместо румынской тяги, чтобы снизить нагрузку на колено.
  Legs (ноги): Приседания со штангой 4×8 → Жим ногами узкой постановкой 4×10 (Приседания со штангой — глубокая осевая на колено. Заменяем на жим ногами узкой постановкой в неполной амплитуде, как и указано в заметке тренера.)
  Legs (ноги): Жим ногами 3×12 → Жим ногами 3×12 (Жим ногами оставляем в плане, но фиксируем неполную амплитуду указанием в комментарии тренеру (в op этого не передать — тренер проследит на месте).)
  Legs (ноги): Румынская тяга 3×10 → Гиперэкстензия с весом 3×10 (Румынская тяга — осевая нагрузка на поясницу и заднюю цепь, дублирует становую из дня спины. Заменяем на гиперэкстензию с весом: задняя поверхность бедра и ягодичные без нагрузки на колено.)
  предупреждение валидатора: в плане остаётся «Жим ногами», противопоказанное при: колени

→ list_pending_proposals()  [0.0 с]
  в очереди 1: program pending — Айдар Ким
```

Итог: программа клиента не изменилась. Черновик ждёт решения в очереди тренера (веб-кабинет или Telegram-бот),
валидатор приложил к нему предупреждение.

## Что показал прогон

- Первый прогон того же запроса (черновик `2abd60fb`, 6,0 с) заменил «Приседания со штангой» на «Бёрпи с
  отжиманием». Упражнение с прыжками при больном колене прошло все фильтры, потому что в каталоге у него не указаны
  противопоказания. Такие поля заполнены у 43 из 196 упражнений. Без противопоказаний, например, «Бёрпи с
  отжиманием», «Фронтальные приседания», «Приседания в смите» и «Выпады со штангой». Поэтому `search_exercises`
  с `avoid_problems_with="колено"` тоже их возвращает (выше в выдаче).
- Черновик не применяется без тренера. Так и задумано: решение по плану принимает человек.
